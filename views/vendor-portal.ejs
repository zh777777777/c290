<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=DM+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: {
              DEFAULT: '#2D8659',
              50: '#E8F5EF',
              100: '#D1EBE0',
              500: '#2D8659',
              600: '#256F4A',
              700: '#1D583B',
              800: '#174B2E',
            },
            accent: {
              DEFAULT: '#F59E0B',
              100: '#FEF3C7',
              500: '#F59E0B',
            },
          },
          fontFamily: {
            sans: ['Inter', 'ui-sans-serif', 'system-ui'],
            display: ['DM Sans', 'ui-sans-serif', 'system-ui'],
          },
        }
      }
    }
  </script>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    h1, h2, h3, h4, h5, h6 {
      font-family: 'DM Sans', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-50 text-gray-900 min-h-screen">
  <%- include('partials/nav', { activePage: activePage }) %>

  <!-- Header Section -->
  <div class="bg-gradient-to-br from-primary-600 to-primary-800 text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
      <h1 class="text-4xl md:text-5xl font-bold mb-4" data-testid="text-page-title">Vendor Portal</h1>
      <p class="text-xl text-primary-100 max-w-2xl">Join our mission to reduce food waste. List your surplus food and help build a sustainable Singapore.</p>
    </div>
  </div>

  <!-- Benefits Section -->
  <div class="bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="w-16 h-16 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Recover Revenue</h3>
          <p class="text-gray-600">Turn surplus inventory into income instead of waste</p>
        </div>
        <div class="text-center">
          <div class="w-16 h-16 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Environmental Impact</h3>
          <p class="text-gray-600">Contribute to SG Green Plan 2030 sustainability goals</p>
        </div>
        <div class="text-center">
          <div class="w-16 h-16 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Reach More Customers</h3>
          <p class="text-gray-600">Connect with eco-conscious consumers across Singapore</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Form Section -->
  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="bg-white rounded-xl border border-gray-200 shadow-lg p-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">Post Surplus Food</h2>
      
      <form id="foodListingForm" class="space-y-6">
        <!-- Vendor Selection -->
        <div>
          <label for="vendorId" class="block text-sm font-medium text-gray-900 mb-2">Your Business</label>
          <select id="vendorId" 
                  name="vendorId" 
                  required
                  class="w-full h-12 px-4 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all"
                  data-testid="select-vendor">
            <option value="">Select your business...</option>
            <% vendors.forEach(function(vendor) { %>
              <option value="<%= vendor.id %>"><%= vendor.name %> (<%= vendor.type %>)</option>
            <% }); %>
          </select>
        </div>

        <!-- Food Title -->
        <div>
          <label for="title" class="block text-sm font-medium text-gray-900 mb-2">Food Item Title</label>
          <input type="text" 
                 id="title" 
                 name="title" 
                 required
                 placeholder="e.g., Assorted Bread & Pastries"
                 class="w-full h-12 px-4 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all"
                 data-testid="input-title">
        </div>

        <!-- Description -->
        <div>
          <label for="description" class="block text-sm font-medium text-gray-900 mb-2">Description</label>
          <textarea id="description" 
                    name="description" 
                    required
                    rows="3"
                    placeholder="Describe the food item..."
                    class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all resize-none"
                    data-testid="input-description"></textarea>
        </div>

        <!-- Pricing -->
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="originalPrice" class="block text-sm font-medium text-gray-900 mb-2">Original Price ($)</label>
            <input type="number" 
                   id="originalPrice" 
                   name="originalPrice" 
                   required
                   min="0" 
                   step="0.01"
                   placeholder="10.00"
                   class="w-full h-12 px-4 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all"
                   data-testid="input-original-price">
          </div>
          <div>
            <label for="discountedPrice" class="block text-sm font-medium text-gray-900 mb-2">Discounted Price ($)</label>
            <input type="number" 
                   id="discountedPrice" 
                   name="discountedPrice" 
                   required
                   min="0" 
                   step="0.01"
                   placeholder="5.00"
                   class="w-full h-12 px-4 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all"
                   data-testid="input-discounted-price">
          </div>
        </div>

        <!-- Quantity -->
        <div>
          <label for="quantity" class="block text-sm font-medium text-gray-900 mb-2">Quantity Available</label>
          <input type="number" 
                 id="quantity" 
                 name="quantity" 
                 required
                 min="1"
                 placeholder="10"
                 class="w-full h-12 px-4 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all"
                 data-testid="input-quantity">
        </div>

        <!-- Pickup Time -->
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label for="pickupTimeStart" class="block text-sm font-medium text-gray-900 mb-2">Pickup Start</label>
            <input type="time" 
                   id="pickupTimeStart" 
                   name="pickupTimeStart" 
                   required
                   class="w-full h-12 px-4 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all"
                   data-testid="input-pickup-start">
          </div>
          <div>
            <label for="pickupTimeEnd" class="block text-sm font-medium text-gray-900 mb-2">Pickup End</label>
            <input type="time" 
                   id="pickupTimeEnd" 
                   name="pickupTimeEnd" 
                   required
                   class="w-full h-12 px-4 rounded-lg border-2 border-gray-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none transition-all"
                   data-testid="input-pickup-end">
          </div>
        </div>

        <!-- Image Selection -->
        <div>
          <label class="block text-sm font-medium text-gray-900 mb-2">Food Image</label>
          <div class="grid grid-cols-3 gap-4">
            <label class="relative cursor-pointer">
              <input type="radio" name="imageUrl" value="Nasi_lemak_food_item_ff62eaaa.png" class="peer sr-only" required>
              <img src="/assets/generated_images/Nasi_lemak_food_item_ff62eaaa.png" class="w-full aspect-square object-cover rounded-lg border-2 border-gray-200 peer-checked:border-primary-600 peer-checked:ring-4 peer-checked:ring-primary-200 transition-all">
            </label>
            <label class="relative cursor-pointer">
              <input type="radio" name="imageUrl" value="Bakery_bread_and_pastries_491a3fb4.png" class="peer sr-only">
              <img src="/assets/generated_images/Bakery_bread_and_pastries_491a3fb4.png" class="w-full aspect-square object-cover rounded-lg border-2 border-gray-200 peer-checked:border-primary-600 peer-checked:ring-4 peer-checked:ring-primary-200 transition-all">
            </label>
            <label class="relative cursor-pointer">
              <input type="radio" name="imageUrl" value="Dim_sum_selection_52cbe93d.png" class="peer sr-only">
              <img src="/assets/generated_images/Dim_sum_selection_52cbe93d.png" class="w-full aspect-square object-cover rounded-lg border-2 border-gray-200 peer-checked:border-primary-600 peer-checked:ring-4 peer-checked:ring-primary-200 transition-all">
            </label>
            <label class="relative cursor-pointer">
              <input type="radio" name="imageUrl" value="Fresh_salad_bowl_933a387a.png" class="peer sr-only">
              <img src="/assets/generated_images/Fresh_salad_bowl_933a387a.png" class="w-full aspect-square object-cover rounded-lg border-2 border-gray-200 peer-checked:border-primary-600 peer-checked:ring-4 peer-checked:ring-primary-200 transition-all">
            </label>
            <label class="relative cursor-pointer">
              <input type="radio" name="imageUrl" value="Chicken_rice_dish_87e4f1a2.png" class="peer sr-only">
              <img src="/assets/generated_images/Chicken_rice_dish_87e4f1a2.png" class="w-full aspect-square object-cover rounded-lg border-2 border-gray-200 peer-checked:border-primary-600 peer-checked:ring-4 peer-checked:ring-primary-200 transition-all">
            </label>
          </div>
        </div>

        <!-- Submit Button -->
        <button type="submit" 
                class="w-full px-6 py-3 bg-primary-600 text-white rounded-lg font-semibold hover:bg-primary-700 transition-all flex items-center justify-center gap-2"
                data-testid="button-submit">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
          </svg>
          Post Food Listing
        </button>
      </form>

      <div id="successMessage" class="hidden mt-6 p-4 bg-green-50 border border-green-200 rounded-lg">
        <div class="flex items-center gap-2 text-green-800">
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <span class="font-medium">Success! Your food listing has been posted.</span>
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>

  <script>
    document.getElementById('foodListingForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = new FormData(e.target);
      const data = {
        vendorId: formData.get('vendorId'),
        title: formData.get('title'),
        description: formData.get('description'),
        originalPrice: formData.get('originalPrice'),
        discountedPrice: formData.get('discountedPrice'),
        quantity: parseInt(formData.get('quantity')),
        pickupTimeStart: formData.get('pickupTimeStart'),
        pickupTimeEnd: formData.get('pickupTimeEnd'),
        imageUrl: formData.get('imageUrl'),
      };

      try {
        const response = await fetch('/api/food-listings', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        });

        if (response.ok) {
          document.getElementById('successMessage').classList.remove('hidden');
          e.target.reset();
          setTimeout(() => {
            window.location.href = '/food-rescue';
          }, 2000);
        } else {
          alert('Failed to post listing. Please try again.');
        }
      } catch (error) {
        console.error('Error posting listing:', error);
        alert('An error occurred. Please try again.');
      }
    });
  </script>
</body>
</html>
